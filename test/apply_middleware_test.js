/**
 * Test case for applyMiddleware.
 * Runs with mocha.
 */
'use strict'

const applyMiddleware = require('../lib/apply_middleware.js')
const assert = require('assert')
const co = require('co')

describe('apply-middleware', function () {
  this.timeout(3000)

  before(() => {

  })

  after(() => {

  })

  it('Apply middleware', () => {

  })
})

/* global describe, before, after, it */

/**
 * Test case for combineReducers.
 * Runs with mocha.
 */
'use strict'

const combineReducers = require('../lib/combine_reducers.js')
const assert = require('assert')
const co = require('co')

describe('combine-reducers', function () {
  this.timeout(3000)

  before(() => {

  })

  after(() => {

  })

  it('Combine reducers', () => {

  })
})

/* global describe, before, after, it */

/**
 * Test case for devTool.
 * Runs with mocha.
 */
'use strict'

const devTool = require('../lib/dev_tool.js')
const assert = require('assert')
const co = require('co')

describe('dev-tool', function () {
  this.timeout(3000)

  before(() => {

  })

  after(() => {

  })

  it('Dev tool', () => {

  })
})

/* global describe, before, after, it */
